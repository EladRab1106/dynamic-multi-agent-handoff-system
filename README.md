# dynamic-multi-agent-handoff-system

to test the system open 4 terminals and run in each of them python -m agents.<"agentname">.run_service
in the fifth terminal run python main.py that builds the graph and starts the flow